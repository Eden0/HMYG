.index {
  color: var(--themeColor)
}
swiper {
  /* swiper 高度默认是写死的,需要重新设置样式 */
  width: 750rpx;
  height: 340rpx;
}
swiper .imageStyle {
  width: 100%; 
}
.nav {
  display: flex;
}
.nav navigator {
  flex: 1;
  /* 挤压图片缩小 */
  padding: 20rpx;
}
.nav navigator image {
  width: 100%;
}